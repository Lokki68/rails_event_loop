
.card.shadow-lg.rounded-lg.p-6
  .card-title.flex.justify-between
    %h2= "Modifier #{@event.title}"
  .card-body
    = simple_form_for @event, url: app_event_path(@event) do |form|
      = render partial: 'form', locals: {form: form}